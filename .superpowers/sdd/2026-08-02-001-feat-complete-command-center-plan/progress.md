# SDD ledger — plan: docs/plans/2026-08-02-001-feat-complete-command-center-plan.md

Recovery baseline 2026-08-08: authoritative workspace is the repository root on branch feat/controlled-code-executor at base HEAD 32c02b1 with a large preserved dirty implementation. Do not reset, checkout, discard, push, or access the owner-authorized real PR #23178 outside the product itself.
U1–U8: substantial implementation present; completion remains subject to final R1–R23 audit rather than recovery-memory claims.
U9: in progress — mirrored PR E2E passed in 239 seconds. GitHub CLI target projection now composes `headRepositoryOwner.login/headRepository.name`; focused adapter/facts tests pass 40/40 and an independent review found no Critical/Important/Minor issues. After a managed restart, the product's own `/api/refresh` completed with HTTP 200 in 163.9 seconds: all 77 PRs had complete atomic Git targets, and system-only PR #23178 was routed into the Work Ledger as a queued `source_root` targeted to `pr-engineer`. The active PR engineer has no explicit `taskBrain`; PR work therefore remains fail-closed at the owner-controlled task-brain gate and must not fall back to its routine brain.
U10: complete in the working tree. Copied-backup/clean-directory restore, drain/writer-lease hardening, and the mandatory trusted 20-owner production migration/validation registry are closed. The registry handles seven legacy transforms, twelve validation/immutable/derived owners, candidate-configuration role validation, and the legacy authority cross-store adoption while preserving unknown/schema-less/retired files uninterpreted. Online and offline runtimes construct the production registry internally, and a compatibility extension cannot delete/add files, mutate protected bytes or archives, replace the candidate root, or bypass future-schema checks. The final repair round was RED/GREEN for extension deletion/rewrite, future memory-index downgrade, replaceable registry wiring, and root identity; independent re-review found no P0/P1/P2. Fresh verification passed 273 U10 tests, including the real PowerShell offline restore, backup/readiness/lock suites, Code Job archives, memory adoption, and every durable activation crash window; syntax and scoped diff checks pass.
U11: documentation and release checks present but release artifacts are untracked; LICENSE is intentionally absent pending explicit owner SPDX choice.
U12: production Issue E2E clock-consistency defect fixed by injecting the runtime clock into ConfirmationQueue; focused confirmation runtime and U12 Issue E2E tests passed. UI validator fix round 1 added explicit initial/stale System API failure fixtures, but scoped re-review found the normal path can still observe a retained backup button before the current status response. Fix round 2/5 is in progress with request-bound synchronization and a normal-path RED fixture. A controller full-validator run also hit an unrelated transient `networkidle` timeout; diagnosis isolated one live `/api/attention/next` request pending for over 23 seconds while direct endpoints later returned in milliseconds. Full regression and remaining acceptance gates require fresh runs.
Recovery tooling note: the plan uses U-ID headings rather than `Task N` headings, so the stock task-brief extractor cannot identify these units; scoped briefs are maintained beside this ledger.
U12 UI validator: fix round 2/5 (1 prior Important addressed, 2 new Important open — successful current response can still read retained UI; held failure fixture lacks route-entry/pending proof; no commits). Controller subsequently completed one full validator run with desktop/mobile, confirmation, Code Job, external Review, initial-503, and stale-503 fixtures all passing.
U12 UI validator: fix round 3/5 in progress under the original implementer, with response-bound successful-render evidence and deterministic held-response latches required by TDD.
U12 UI validator: fix round 3/5 (3 addressed, 0 open — successful responses are bound to the rendered `readiness.checkedAt`; held 503/200 fixtures prove route entry and pre-release pending state; corrected replay-verified review package removed a tooling truncation artifact; no commits).
U12 UI validator subtask: complete (working-tree change in `scripts/validate-ui.mjs`, scoped review clean with 0 Critical/Important findings). Controller verification passed `node --check scripts/validate-ui.mjs`, `git diff --check -- scripts/validate-ui.mjs`, and the complete `node scripts/validate-ui.mjs`: desktop/mobile, confirmation queue, Code Job controls/packages, external Review confirmation, initial 503, retained-success/current-503, and retained-success/current-200 all passed with no console/page errors or overflow.
U10 production migration design: the five inventory ambiguities are resolved without an owner blocker. The mandatory trusted 20-owner registry runs before and after any compatibility extension; candidate-only authority projection adoption writes the normalized journal while retaining the legacy file; candidate configuration controls declared role-state validation while retired keys remain inert; schema-less caches/workspaces are preserved-unvalidated; Code Job archives are immutable-validation-only. Implementation is four TDD slices and no production default may be installed before all 20 owners are represented.
U10 drain/writer-lease subtask: complete in the working tree after four bounded review/fix rounds. The original review found 1 Critical and 2 Important lifecycle defects; rounds 1–3 closed construction-owner, deadline, failure-history, late-publication, and bounded/coalesced writer-release races, then round 4 closed the remaining same-`Error` event-cardinality Minor. Controller verification passed the exact regression, `node --check` for CLI and test, scoped `git diff --check`, and the five-file focused suite with 185/185 passing. Final immutable re-review is READY with 0 Critical/Important/Minor findings. Restore, drain/lease, and the later trusted 20-owner migration registry are all closed.
CLI brain decision 2026-08-08: owner selected the recommended first-release shape. The controller reasoning role and employee `brain`/`taskBrain` may select Codex CLI or Claude CLI, implemented as one supervised invocation per claimed task; durable memory/context remains in MyDashboard, CLI output remains untrusted, and all repository/GitHub effects stay behind controlled execution and sequential confirmation. Persistent cross-task CLI sessions are deferred and are not a first-release completion gate.
U9 blocked/pending PR-source recovery subgate: complete in the working tree after two independent review/fix rounds. Source cutover releases only side-effect-free `decision_attempts_exhausted`; every failure after entering `submit_delivery`, including a failed owner-attention persistence/recovery path, remains `delivery_failed` / `delivery_attempts_exhausted` and therefore fail-closed. The regression was observed RED then GREEN, final independent re-review found 0 Critical/Important/Minor issues, and fresh controller verification passed 346/346 plus four syntax checks and scoped diff validation. This closes the generic recovery defect only; product-only acceptance of real PR #23178 remains reserved for the later controlled runtime gate.
Acceptance checkpoint 2026-08-09: local commit `50f816d` integrated the preserved U1-U10/U9/CLI work without pushing. Post-commit `npm test` passed 2777/2784 with zero failures and seven documented skips; distribution/clean-room passed 14/15 with one live process-guard skip; 435 syntax checks, both Edge fixtures, and desktop/mobile UI validation passed. The supervised CLI focused/cross-layer gate passed 369/370 with one Windows symbolic-link skip; Codex 0.147.0 and Claude 2.1.222 were exercised only with fixed `--version`; two final reviews reported Ready and zero P0/P1/P2. U11 remains owner-blocked only on the explicit SPDX license choice. U12 still reserves live PR acceptance for an owner-authorized run by MyDashboard itself; Docker was not rerun because its daemon was stopped, but the previously passing adapter/test are unchanged. No model call, service restart, real PR/GitHub action, or push occurred.
Completion audit continuation 2026-08-09: authoritative branch is clean at `740c948`. Independent read-only U9/U10 and U11/U12 evidence audits are running as agents `019fe2ac-4def-7681-9478-6aad8caec561` and `019fe2ac-5237-7763-8c29-87c6d57f6c8c`. Safe loopback inspection found the managed runtime ready on stored/runtime-effective configuration v3, but that active version and its newest draft both have no PR Engineer `taskBrain`; active PR permissions are legacy Review-only and `codeExecutor`, `changePackages`, and `githubActions` are disabled. The configured Ark provider references an environment variable absent from the controller shell. `gitTargetAvailable` remains derived GitHub fact data, not a configuration switch. No runtime/configuration mutation, refresh, task claim, GitHub/PR access, model call, service restart, or push was performed.
Final review 2026-08-09 at `9f3b54a`: two fresh read-only reviewers found 0 P0, 3 P1, 4 P2, and 1 P3. P1/P2 repair is mandatory before final acceptance. Review fix 1 is in progress for supervised CLI protected-root separation, per-invocation identity revalidation, and bounded deadline-owned cleanup. Brief: `review-fix-1-cli-isolation-brief.md`. P3 server/test modularization is recorded as non-blocking debt and must not trigger a risky late behavior refactor.
Review fix 1 initial implementation `e532032`: task review failed spec/quality with 3 Critical and 5 Important findings (Windows root trust/create race; pathname cleanup TOCTOU; reap residue not root-blocking; stale partial mutation; cross-kind/root serialization and close failure; incomplete/injectable production roots; post-deadline settlement; load-bearing test gaps). Fix round 1/5 is assigned back to the original implementer; review record: `review-fix-1-cli-isolation-review.md`.
Review fix 1 round 1/5 (`e532032..c0ae694`): 3 findings addressed, 5 remain open (Windows ACL/absolute root creation; pathname cleanup TOCTOU; whole-multi-tree stale plan; forgeable production capability; production-path race tests), with 1 new Important POSIX cancellation handle leak. Scoped record: `review-fix-1-cli-isolation-rereview-1.md`. Round 2/5 is assigned to the original implementer.
Review fix 1 round 2/5 (`c0ae694..0a171bd`): independent review found 1 Critical and 3 Important. Same-handle cleanup, complete multi-tree stale preflight, and the Windows-x64 production gate are addressed; parent ACL trust is still pathname/ABA-raceable, composition authority remains importable/forgeable, production tests miss those paths, native cancellation can release the root lease before handles close, and real process reaping can exceed the one total deadline. Scoped record: `review-fix-1-cli-isolation-rereview-2.md`. Round 3/5 is assigned back to the original implementer; no later task may start implementation until this task review is clean.
Review fix 1 round 3/5 (`0a171bd..9a1ca9a`): parent ACL/creation handle binding and READY cancellation settlement are addressed, but fresh review found 4 Important issues: grant-free injected wrappers can still delegate to incomplete production construction; a global activity tail breaks staggered normal concurrency; the ACL ABA regression can pass without proving the vulnerable interval was reached; and the deadline test substitutes the process boundary while never-settling/application-close cases are absent. Regression A-F pass and G fails. Scoped record: `review-fix-1-cli-isolation-rereview-3.md`. Round 4/5 must use a fresh more-capable implementer.
Review fix 1 round 4/5 (`9a1ca9a..4c93e8e`): the lexical grant, healthy staggered concurrency, deterministic native ACL mutation proof, real Windows child, never-settling activity, and workforce application-close path are implemented and controller tests pass 141/144 with zero failures and three privilege skips. Fresh review found 3 Important lifecycle races: non-versioned/versioned Code Job brain directories discard or cache provider close authority; pre-root generate can outlive a successful close; and a sibling already past recovery can spawn after another process marks the shared root blocked. Scoped record: `review-fix-1-cli-isolation-rereview-4.md`. Round 5/5 requires a fresh more-capable implementer and a minimal scope expansion to Code Job brain lifecycle ownership.
Review fix 1 round 5/5 (`4c93e8e..f9e1d41`): controller union passed 195/198 with zero failures and three privilege skips, including real ACL/Windows child and the final async races. Final immutable review accepts directory retention, provider close linearization, and root admission, but found 2 remaining Important acceptance blockers: close deduplication is by configured wrapper rather than underlying provider owner identity, and application lifecycle tests bypass the real default non-versioned/versioned Code Job ownership paths. Record: `review-fix-1-cli-isolation-final-review.md`. Continue as a new narrow review-fix-1b task rather than exceeding the five-round task loop.
Review fix 1b (`f9e1d41..ad2c06c`): provider-owner identity deduplication and real default non-versioned/versioned Code Job lifecycle/startup-failure coverage are implemented; controller verification passed 201/204 with zero failures and three explicit Windows symbolic-link privilege skips. Independent static review found 4 Important and 1 Minor follow-ups: immutable close capture, partial-construction cleanup, bounded fixture setup process trees, failure-safe real-child teardown, and explicit reverse-order proof. Record: `review-fix-1b-cli-owner-dedup-default-lifecycle-review.md`. Continue as a fresh narrow review-fix-1c task; no later implementation task may overlap it.
Review fix 1c initial implementation (`ad2c06c..77b6816`): focused tests passed 98/98 and the six-file CLI union passed 209/212 with zero failures and three existing privilege skips. Bounded fixture setup and failure-safe real-child teardown are accepted, but adversarial probes and independent review found 2 Important plus 1 Minor ownership defects: primitive/reused thrown values lose or overwrite cleanup protocols, current-provider boundary validation can fail before admission, and a first non-closable admission caches stale null after close is later added. Record: `review-fix-1c-cli-construction-cleanup-fixture-bounds-review.md`. Round 1/5 returns to the original 1c implementer; no later implementation may overlap it.
Review fix 1c round 1/5 (`77b6816..e1820f3`): explicit construction tokens, FIFO consuming cleanup queues, admission-before-validation, and stale-null recapture are implemented; focused tests passed 103/103 and the six-file union passed 214/217 with zero failures and three existing privilege skips. Independent review accepts A–D but found 2 Important coverage gaps: ordinary no-token configured factory calls still lose retry ownership, and default non-versioned/lazy-versioned Code Job construction does not propagate or adopt the token before successful directory registration. Record: `review-fix-1c-round1-review.md`. Round 2/5 returns to the original implementer with minimal scope expansion to Code Job directory/authority lifecycle ownership.

Review fix 2: complete (commit `01feb27`; focused workforce suite 172/172; fixed review package SHA-256 `162C860C430B58215366696BEC3A2614B2A1A3FD0D1FAAEB2C7B7832A5494380`; independent review clean with 0 Critical/Important/Minor). Structured-provider setup failures durably route to owner attention.
Review fix 3: complete (commits `48a0950..68fc5c7`; manager 33/33 and restore runtime 12/12; round-1 package SHA-256 `CD958778CA6CD4AF6E84F25B5A3ACAFF35F431A10F15DBD25F5CD6AC0FF3FADB`; independent re-review clean). Occupied or indeterminate restore listeners fail closed and managed Status requires readiness.
Review fix 4: production change complete (commit `5ed00ff`; focused external-action suite 37/37; package SHA-256 `D8B50A61E12FB7911431AA0BBE451BAB1CFA4109A74FF7C5EF7D15483DDD22C0`; independent diff review approved). Controller resolved the review's cross-task warning as a real stale U9 fake after the parent mirror failed 0/1, so completion depended on review fix 4b.
Review fix 4b: complete (commit `d6ddaf3`; parent mirror RED 0/1, head mirror GREEN 1/1, external-action suite 37/37; package SHA-256 `EA5A146477F856BE258B282D65100A634D2AA7E07BFEF45ABC1A4C592E2FB285`; independent review clean with 0 Critical/Important/Minor). The U9 fake now emits exact Review `COMMENT` and confirmed Head evidence without topology or production changes.
Review fix 4 gate: complete (commits `68fc5c7..d6ddaf3`, production and cross-layer reviews clean).
Review fix 5: complete (commits `ee953d4..c19af62`; strengthened mirrored U9 scenario passed 1/1 in approximately 112 seconds; cumulative package SHA-256 `EF77BB529B745CDE69676ADB6645103E33D17A7C291AE81B2C8379B5D38E1CE7`; independent Round 2 review approved with 0 Critical/Important/Minor). Durable evidence now proves exclusive developer/tester/PR-engineer actors, exact confirmation bindings, version/digest-bound deliveries, and both-Head PR-engineer reevaluation through restart.
Review fix 6: production compatibility fix complete (commit `4105032`; focused GitHub adapter/facts gate passed 41/41; package SHA-256 `47E7B98E7757BC9332977F679509544701917087EC80AA319B0FDE73C09DFEDB`; independent review approved with 0 Critical/Important/Minor). The dedicated `gh pr view` target parser now accepts the valid minimal owner `{ login }` shape while retaining strict key/data boundaries, exact atomic target comparison, and the separate GraphQL contract. Product-only runtime acceptance remains pending after a managed restart.
Review fix 7a: exact owner decision-retry core complete (commits `cdbfe7f..d8442bb`; service 7/7, service-plus-ledger 182/182, focused HTTP 1/1, server-plus-composition 178/178; cumulative package SHA-256 `1C0702925C85EB8D6D87F65143AEC0784E12BEBEFB75647F8DE5533A60932C79`; independent Round 1 re-review approved with 0 Critical/Important/Minor). Only the exact current blocked PR source-root `decision_attempts_exhausted` state can be requeued through a revision/input-digest-bound same-origin request, with fixed owner transition authority and background agency signaled only after durable success. Review fix 7b still owns the Work-view control.
Review fix 8: Ollama PR employee structured-output compatibility complete (production commits `45726b2`, `91284ba`, `b199a73`; focused adapter 26/26 and related employee/confirmation group 87/87; cumulative package SHA-256 `2E661F43A99FA83F85AC9943DC65BE2C6F9C2659345DB7A34039848EF02FA6F4`; independent Round 2 cumulative re-review approved with 0 Critical/Important/Minor). Ollama 0.32.6 synthetic-only live acceptance completed both short and pressure cases in one attempt under 90 seconds, with exact JSON contracts, deterministic prompt/output reserves, useful bounded diff evidence, fail-closed truncation, and sanitized errors. MyDashboard runtime and all real PR/GitHub data remained untouched.
Review fix 7b: Work-view exact owner retry control complete (commits `e59cd43`, `30c8c21`, `7e7c2eb`, `b60c78f`; focused Work tests 39/39 and Work-plus-graph 48/48; cumulative package SHA-256 `29E8E673458C48B007864E7281938C6A01859F3BC6A26722DA217CD4AF363C5E`; independent Round 3 re-review approved with 0 P0/P1/P2). The browser exposes only the exact decision-exhaustion retry, derives revision/digest authority from a freshly revalidated fail-closed projection, serializes requests, sequences reloads and late responses, and rejects accessor-backed, custom-prototype, duplicate-ID, and malformed collection shapes without granting generic execution authority. Runtime/browser acceptance remains HEAD-bound and pending the managed restart.
GitHub CLI login / PR updated-window continuation 2026-08-12: both written specifications are owner-approved. Active focused Task 1 adds credential-mode configuration/UI from base `dbac5f8`; implementation remains TDD-scoped, the complete U12 is reserved for final stable HEAD, and no real GitHub write or push is authorized.
GitHub credential configuration Task 1 complete 2026-08-12 (commits `0dee05d`, `6a9e28c`): controller cumulative scope passed 186/186 plus syntax/diff checks; independent cumulative re-review found 0 Critical/Important/Minor. Legacy omission, explicit modes, 600-second transport consistency, restart/security impact, and real conditional form switching are locked. Task 2 starts from `6a9e28c`; no `gh` invocation or GitHub write occurred.
Paused checkpoint 2026-08-12 at `0301c1c`: Task 2 implemented the unintegrated GitHub credential-source foundation and controller verification passed 61/61 plus syntax/diff checks without invoking real `gh`. Security review found no finding, with the documented residual that JavaScript strings cannot be physically erased. Reliability review found two unresolved Important blockers in `src/adapters/gh-login-credential-source.js`: a directory created before `privateDirectoryManager.prepare` rejects can escape proven cleanup, and cleanup-session preparation is not bounded by the caller signal/shared total deadline. Required RED coverage is create-then-fail/cancel cleanup, blocked cleanup-session cancellation/deadline, and production-shaped descriptor/runner/directory integration. Per owner request, preserve HEAD and stop before fixes, Task 3 integration, runtime configuration, service restart, real credential access, or any GitHub action.
Task 2: fix round 1/5 (1 addressed, 2 open — post-create prepare failure cleanup is now proven; caller-owned bound for an uncooperative cleanup-session preparation and its deterministic coverage/report commands remain open; commits `0301c1c..e8eca13`). Controller rerun of credential source, known CLI locator, and supervised runner passed 66/66 plus syntax/diff checks; no real `gh`, credential, model, service action, network, GitHub action, or push occurred.
Task 2: fix round 2/5 (2 addressed, 0 open — caller-owned cleanup-preparation bound and deterministic uncooperative dependency/report coverage are complete; commits `e8eca13..0e44081`). Controller rerun passed credential source 37/37 and Windows private-directory/cleanup 8/8 plus syntax/diff checks; independent scoped re-review found no new Critical/Important breakage.
Task 2 complete 2026-08-13 at `0e44081`: the shared token-env and gh-login credential-source foundation now proves fixed executable identity, bounded one-shot token acquisition, mandatory process/directory cleanup, caller-owned cancellation/deadline settlement, and production-shaped Windows behavior without invoking installed `gh`, host credentials, models, services, network, GitHub, or push. Task 3 owns consumer/composition integration; no runtime credential access is enabled by Task 2 alone.
Task 3 complete 2026-08-13 (commits `471c66c`, `92b0235`, `d4d02de`, `a30a682`): Review and generic confirmed PR actions share exactly one configured credential source; acquire/use/release, actor and target revalidation, one total deadline, close cancellation, unknown-result recovery, private gh-login runtime isolation, pending/read/reject zero-acquire behavior, and startup cleanup are implemented without raw provider or executor bypasses. Controller evidence passed composition/runtime 104/104, queue 52/52, and the Task 2/3 cumulative group 226/226 plus syntax/diff checks. Independent scoped review found no actionable Critical or Important issue; one Minor cancellation-latency observation for update-branch polling remains assigned to final runtime timing acceptance. No installed `gh`, host credential, model, service, network, real GitHub action, or push was used.
U9 live owner-request intake reliability: complete in commit `18d2a97`. A schema-v2 product-only request for PR #23178 resolved and routed, then reproducibly failed ledger intake because a prior completed automatic PR root at assignment sequence 1991 received trusted `pull_request.left_scope`; the lifecycle authority fence produces blocked/in-progress graph state while the graph transition gate formerly permitted only completed-to-pending reactivation. The correction derives an exact ledger-only completed-root scope-exit exception while retaining downstream PR-source validation. Controller verification passed the ledger/graph group 260/260, and a read-only in-memory replay advanced all 81 current assignments from cursor 1988 to 2069 with byte-identical input files. Independent review approved the task with 0 Critical/Important/Minor findings. Managed runtime cutover and product-only PR acceptance remain later gates; no GitHub/PR write or push occurred.
U9 live owner-request cross-root settlement: implementation complete in commits `ee4c54d` and `f9fe20b`. The product accepted request `784966d8-8cfb-4bc5-95b0-6f1ba8add4ce` as work item `work-item-9fb9fcc4...`, then correctly fenced it behind a prior rejected proposal but exposed a missing terminal-settlement path. TDD now requires the latest retained digest-bound `proposal_result_applied` proof for both fresh and persisted rejected-proposal settlement, indexes the effective timeline once per transaction including same-batch events, and fails closed for rotated proof, forged self-consistent context, later transitions, and child/root variants. The ledger/source/graph group passed 307/307, full `npm test` exited 0 with clean-room distribution 15/15, current production data reconciles successfully only in a read-only in-memory store with byte-identical inputs, and cumulative independent re-review closed 3 Important plus 1 Minor with no remaining findings. Runtime application remains pending an exact managed-process recovery; no GitHub/PR write or push occurred.
U10 managed shutdown drain reliability: implementation complete in commit `2310e1f`. A real managed restart proved the former 30-second server drain raced a 34-second atomic unified-memory projection; the authenticated failure receipt retained the exact process and writer guard as designed. Defaults are now bounded at 90 seconds for service drain and 120 seconds for manager observation, with a 30-second terminal-receipt margin. Lifecycle/server/script verification passed 123/123 plus syntax/PowerShell AST/diff checks, and independent review found 0 Critical/Important/Minor. A private recovery snapshot captured 29 top-level data files (68,896,173 bytes) with stable source/copy SHA-256 and manifest digest `7f857546c159d7d473311ce9d1f2c063b8d30c681c66dbe556f55bccf1b45b95`. Exact termination of the now idle, listener-free retained PID and evidence-preserving control-state recovery require explicit owner authorization before runtime cutover; no force-kill or evidence deletion has occurred.
U10 preserved snapshot total compatibility complete 2026-08-14 (commits `51bac34`, `8d95bc3`): the owner-authorized real recovery attempt from `4764ecd` failed safely before termination because the preserved manifest's mathematically integral derived total is serialized as `68896173.0` (Decimal in Windows PowerShell 5.1, Double in PowerShell 7). TDD now permits only finite integral Decimal/Double values for top-level `totalBytes`, while strict per-file integers, 512 MiB bound, and exact recomputed-sum equality remain unchanged. Cross-version focused tests passed 5/5; the serial recovery aggregate passed 76/78 with zero failures and two Windows EPERM file-symlink skips; process/control leakage was zero. Independent review approved with 0 Critical/Important/Minor. The real PID/control/snapshot remained intact and its manifest digest unchanged; retry requires renewed owner authorization because the original execution authority was bound to `4764ecd` and candidate HEAD is now `8d95bc3`.
U12 GitHub gh-login cross-volume startup correction complete 2026-08-14 (commits `9d7c90d`, `5533994`): managed v9 startup exposed a Windows-only false overlap because `path.relative(C:, D:)` is absolute; containment now accepts only non-absolute relative results while exact and same-volume ancestor/descendant roots remain rejected. The exported configured credential factory regression was RED then GREEN without invoking real `gh`, credentials, network, models, services, or GitHub. Controller verification passed 120/120 plus syntax and diff checks; independent task review approved with 0 Critical/Important/Minor. Managed startup and product-only reconciliation/acceptance remain the next gates; no GitHub action or push occurred.
U12 Windows private-directory optional-signal startup correction complete 2026-08-14 (commits `aa6959c`, `fa45c45`): the next managed startup reached real ACL inspection but Node rejected the optional `null` signal passed to `execFile`. The production Windows-x64 regression was observed RED 0/1 with `ERR_INVALID_ARG_TYPE` and GREEN 1/1; `runPowerShell` now omits only absent signals while preserving supplied-signal cancellation and non-null validation. Controller verification passed 194/196 with zero failures and two documented Windows file-symlink privilege skips, plus syntax/diff checks; independent review approved with 0 Critical/Important/Minor. No CLI/model call, credential read, GitHub action, network write, or push occurred.
U12 GitHub managed private-runtime startup correction complete 2026-08-14 (commits `d3b2e9e`, `98c0e89`, `b40afda`, `455d36f`): real ACL evidence showed the home-directory implementation violated the approved private-runtime design and was correctly rejected because another SID has inheritable write rights. Managed startup now passes its authenticated project-digest runtime directory to composition; `gh-login` prepares only its fixed `github-credentials-v1` child, while token-env remains directory-free. Focused wiring/path tests were RED 0/2 and GREEN 2/2; the cross-layer group passed 316/318 with zero failures and two documented Windows file-symlink privilege skips, and final composition verification passed 84/84. Independent review found no Critical/Important issue; its sole dead-code Minor was removed and re-review found all findings addressed with no new breakage. No ACL mutation, CLI/model call, credential read, GitHub action, network write, or push occurred.
U12 GitHub production credential-factory startup correction complete 2026-08-14 (commit `e33be33`): managed startup exposed that the shipped gh-login factory passed Node's special `process.env` object and a runner class instance into its own strict plain-port validator. The exact production-factory regression was RED 0/1 with the runtime `TypeError` and GREEN 1/1 after the factory copied only five allow-listed non-secret system path variables and exposed only a frozen bound `run` port; the validator was not relaxed. Controller verification passed credential source 38/38, composition root 84/84, and supervised runner 15/15 plus syntax/diff checks. Independent review found 0 Critical/Important/Minor and approved managed restart. The synthetic fixture was only pinned, never executed; no installed CLI, credential read, model call, GitHub action, network write, ACL mutation, or push occurred.
U12 GitHub CLI token line-ending correction complete 2026-08-14 (commit `27ba329`): after managed v9 startup, the product itself attempted all seven historical uncertain Review reconciliations but the credential parser rejected each official `gh auth token` result as `GITHUB_CREDENTIAL_OUTPUT_INVALID`. GitHub CLI's official implementation terminates the token with LF, while MyDashboard accepted no terminator or CRLF and explicitly rejected LF. The synthetic LF regression was RED at the new 38-byte case and GREEN after `parseToken` removed exactly one LF or CRLF before unchanged strict validation. The credential/confirmation/composition/external-action group passed 305/305 plus syntax/diff checks; independent review found 0 Critical/Important/Minor and approved restart. No real token was exposed, no installed gh was invoked directly, and no model call, GitHub write, ACL mutation, or push occurred.
U12 historical unknown external-action sealing complete 2026-08-14 (commits `8c5efb7`, `9d89949`, `fe2cef9`, `a4f8810`): after product-only reconciliation safely resolved five of seven historical uncertain Reviews, two exact marker-absent outcomes remained unknowable and non-retryable. The unified queue now surfaces them only as explicit owner-resolution items with a reject-only “承认结果未知并封存” path; it never displays approve/retry/publish controls and never invokes credentials, models, executors, or GitHub. Sealing preserves the unknown execution/failure and durable owner rejection across restart, projects only a fixed `seal_unknown_and_forbid_replay` decision plus time across public history and sanitized unified memory, and omits free-form reason from the sealed public projection. Malformed partial evidence and malformed rejected-memory projections fail closed. Verification passed 247/247 expanded, 453/453 affected-consumer, 98/98 final focused, Chromium, syntax and diff gates; final independent review found 0 Critical/Important/Minor and approved managed restart. One whole-suite run exposed and corrected a legacy hidden-unknown test expectation; an unrelated load-sensitive staggered CLI test then passed focused 20/20 and remains a final whole-suite rerun gate. No live item was auto-cleared and no GitHub write or push occurred.

# MyDashboard 开发计划

> 当前执行与验收的唯一权威计划是 `docs/plans/2026-08-02-001-feat-complete-command-center-plan.md` 中的 U1–U12。本文件保留早期阶段名称用于历史追踪；截至 2026-08-10，U1–U10 与 CLI 大脑的本地实现和自动验收已收口，U11 的 Apache-2.0 许可证切片已完成；U12 与产品自身的真实 PR 授权验收仍未完成。

## 目标

把 MyDashboard 建成一个本地优先的综合指挥中枢。系统中的岗位能够主动观察、判断、反馈、咨询和持续跟进；每个岗位可以独立选择本地 Ollama 或第三方大模型；涉及外部写入和高风险执行的动作统一进入逐项确认队列。

## 开发原则

- 一次只推进一个可验收阶段，当前阶段未通过测试和运行验证前，不进入下一阶段。
- 岗位判断、工具执行和外部授权相互隔离，模型不能绕过权限边界。
- GitHub 评论、Review、Approve、合并等外部写操作默认必须由用户逐项确认。
- 保留现有本地数据和兼容接口，不因架构升级丢失员工状态、作业或记忆。
- 未确认最终 GitHub 账号前，不推送代码、不创建 PR。

## 阶段路线

| 阶段 | 状态 | 交付物 | 完成标准 |
| --- | --- | --- | --- |
| 1. 通用员工控制层 | 已完成（2026-08-01） | EmployeeController、EmployeeRegistry、动态员工 API、PR 员工迁移 | 两个模拟岗位可独立发现/运行/暂停；旧 PR API 与状态兼容；全量测试通过；本地服务验证通过 |
| 2. 受控代码执行器 | 已完成（2026-08-02） | 隔离工作区、命令策略、执行日志、产物和失败恢复 | 能在测试仓库完成读取、修改、测试；越权命令被拒绝；全过程可追溯 |
| 3. GitHub 行动与确认队列 | 已完成（2026-08-02） | 固定 Head 的 PR Review 适配器；逐项弹框确认；幂等、失效与恢复 | 未确认不产生外部写入；确认后只执行展示的动作；重复提交不会重复写入 |
| 4. 可配置工作流与路由 | 已完成（2026-08-02） | 事件节点、条件规则、岗位/人员目标、优先级、兜底和试跑解释 | PR、Issue 等工作可按配置分派；冲突和未命中有明确结果；规则变更可追溯 |
| 5. 主动工作循环 | 已完成（2026-08-02） | 事件触发、定时巡检、咨询、等待与持续跟进 | 岗位可主动创建工作、提出问题、等待回复并在条件满足后继续 |
| 6. 多岗位与多大脑 | 已完成（2026-08-02） | 主控、需求、PR、开发、测试岗位；Ollama 和第三方模型路由 | 岗位职责、权限、状态和模型配置彼此独立，并可由统一运行循环调度；真实全局主控在阶段 8 完成 |
| 7. 代码岗位安全闭环 | 已完成（2026-08-03） | 代码授权、Code Job、结构化工具循环、变更包、记忆回写与端到端恢复 | 开发/测试可在逐项授权后完成隔离修改与验证；撤权、暂停、超限和崩溃均有确定结果 |
| 8. 共享任务树与真实主控 | 已完成（2026-08-03） | 父子任务、依赖、版本化交付物、拆解派工、监督、退回、催办和升级 | 主控能够协调需求、开发、测试和 PR 岗位完成同一共享目标，而不是只处理单个事件 |
| 9. 记忆大脑与配置中心 | 已完成（2026-08-06） | 更多本地记忆来源、带引用的 Ollama 问答、岗位/模型/路由/权限的版本化配置 | 无模型时仍可检索；模型回答可追溯；配置可校验、试跑、激活、回滚和审计 |
| 10. 完整 PR 工程师 | 实现完成，实网验收待授权（2026-08-09） | 本地 Git、CI、冲突、新 Head 复核、联系协作方和受控 GitHub 行动 | 冲突修改、验证、受控提交、强模型分流、恢复和逐项确认均已组合并通过镜像/生产形态测试；真实 PR 只允许由系统在所有者确认后验收 |
| 11. 运维、开源与综合验收 | 收尾中（2026-08-09） | 备份恢复、就绪检查、日志、操作手册、许可证、开源文档和全链路验收 | 恢复、整树隐私、干净发行与关键端到端门禁已通过；最终 HEAD 绑定全量/浏览器/Docker/独立审查仍在收口，并需所有者选择 SPDX 许可证及授权产品自身的真实 PR 验收 |

## 可配置路由要求

- 事件节点至少覆盖 PR 创建、更新、完成，以及 Issue 创建、分类和状态变化。
- 条件可组合仓库、作者、负责人、标签、Issue 类型、PR 类型、草稿状态、文件范围、CI 状态和风险等级。
- 目标可以是岗位、具体人员或后续流程节点，并支持优先级、启停、命中后停止或继续匹配以及默认兜底。
- 路由试跑必须返回命中的规则和分派理由，配置版本与每次分派结果都要保留审计记录。
- 路由只决定工作交给谁，不得绕过外部写操作和高风险执行的逐项确认。

## 主动工作循环要求

- 路由分派进入持久化工作台账；同一分派只能领取一次，崩溃后可恢复，租约过期后才允许安全重试。
- 岗位可以主动观察、形成判断、提出咨询、等待答复、等待外部条件并在条件满足后继续，而不是只响应页面按钮。
- 咨询与需要用户决定的动作进入统一待处理队列，页面一次只弹出一项；用户答复必须绑定请求修订号和内容摘要。
- 模型只能生成结构化判断和工具意图，不能自行扩大权限；GitHub 写入继续走阶段 3 的确认队列，代码动作继续走阶段 2 的固定语义执行器。
- 每次领取、判断、咨询、答复、重试、移交和完成都写入审计时间线，并进入统一可搜索记忆。

## 多岗位、多大脑与记忆要求

- 首批岗位至少包括主控、需求分析、PR 推进、开发和测试；各岗位有独立职责、能力、暂停状态、调度频率和模型配置。
- 大脑 Provider 使用统一结构化调用契约；本地 Ollama 是默认选项，同时支持 OpenAI 兼容的第三方接口，密钥只从指定环境变量读取。
- 发送代码、需求或记忆到非本机 Provider 必须由岗位配置显式授权；界面持续显示所用 Provider、模型和数据是否离开本机。
- 统一记忆同时保存岗位事件、工作流事件、工作台账和已有 PR 员工记忆，支持关键词、岗位、仓库、事件类型和时间范围查询。
- 记忆索引与原始记录均保存在本机，重建索引不能丢失原始记录；远程模型不是检索可用性的前提。

## 最终目标补充要求

- 主控不是普通岗位的别名：必须能读取全局工作、拆解父任务、管理依赖、派工/改派、监督、催办、接受或退回交付物，并把需要所有者决定的事项升级到统一确认队列。
- 所有岗位围绕同一个版本化任务树协作；需求、代码、测试、Review 和用户决策都作为可验证交付物追加到共享工作，不通过复制任务或孤立文本传递责任。
- 统一记忆除工作台账和旧 PR 记忆外，还要覆盖 Code Job、确认与用户决策、需求版本、Git 变更、测试、PR/CI 事实，以及用户明确选择导入的本地会话历史。
- 记忆问答先进行本地检索，再由 Ollama 或获准的第三方大脑生成带原始记录引用的回答；原始本地记录始终是权威，模型摘要可以重建和纠正。
- 路由、岗位、模型、权限、工作区和确认策略都要在界面中以版本化配置管理，支持草稿、校验、试跑、影响预览、激活、回滚和审计。
- Code Job 初始确认只授权展示的本地范围；文件、工具、网络、Provider、账号、仓库、分支/Head 或权限范围扩大时必须重新确认。应用到真实源仓库和所有外部动作始终单独确认。
- PR 工程师必须持续负责到终态，覆盖相关性判断、固定 Head 分析、修改、测试、CI、冲突、新 Head 复核、作者沟通及待确认的 Review、评论、分支更新、推送和合并。
- 大脑按风险分级：Ollama 只承担日常巡查、相关性判断、摘要和低风险分流；真实 PR 的需求理解、冲突处理、代码修改、Review 与 CI 诊断必须使用显式配置的高能力任务大脑，缺少 Provider、凭据引用或代码数据授权时等待用户，不得静默降级到本地小模型。
- 未确认最终 GitHub 账号前，所有远程写入继续关闭；允许完成本地实现、模拟验收和本地提交，但不推送、不创建 PR、不更新远程分支、不合并。
- 项目运行不依赖也不启用 `claude-mem`；只借鉴其设计思想，自建本地记忆、索引、引用和导入能力。
- 最终交付必须满足开源发布条件，包括许可证、可复现安装、示例配置、贡献/安全/隐私说明、备份恢复和完整操作手册。

详细的需求追踪、关键技术决策、实施单元、测试场景和完成定义见 `docs/plans/2026-08-02-001-feat-complete-command-center-plan.md`。

## 阶段 1 验收清单

- [x] 用行为测试定义通用控制器和注册表的边界。
- [x] 通用控制器持久化 `paused` 和 `revision`，并合并并发运行请求。
- [x] 注册表支持多个岗位、按 ID 路由，并拒绝重复或未知岗位。
- [x] PR 员工迁移到通用控制器，保留 `tick()` 等兼容入口。
- [x] 保留原有 `pr-employee-state` 和 `pr-employee-memory` 数据键。
- [x] 增加 `/api/employees` 和动态岗位控制/运行接口。
- [x] 全量自动化测试通过（96/96）。
- [x] 重启本地服务并验证真实状态、接口和页面。
- [x] 完成代码简化与独立评审，修复评审确认的问题。

## 阶段 1 验收结果

- PR 员工已迁移到通用员工控制层，原有暂停状态、修订号、18 条作业和记忆均未丢失。
- 岗位注册表支持发现、独立运行、暂停/恢复、启动巡检和各岗位独立定时。
- 新增动态岗位 API，同时保留原有 PR 员工接口和仪表盘响应。
- 桌面与手机页面验证通过，无控制台错误和页面溢出。
- 第 1 阶段已经本地提交；后续阶段使用独立分支继续开发，仍不推送 GitHub。

## 阶段 2 验收清单

- [x] 只接受固定的读取、搜索、写入、测试和封存语义动作，不提供任意 shell 命令入口。
- [x] 从可信源仓库创建过滤后的临时副本，源仓库在执行期间保持不变。
- [x] 写入使用文件摘要和工作区版本双重保护，测试和封存重新扫描实际文件系统。
- [x] Docker 测试固定镜像 digest、禁用网络、只读挂载工作区，并清理残留容器。
- [x] 动作输入、输出、stdout、stderr、证明和变更清单使用内容寻址的不可变审计产物。
- [x] 启动恢复区分确定成功、确定失败和副作用不明；不明状态中断并等待安全恢复。
- [x] 恢复后的岗位可通过完整性校验接口取回历史动作结果。
- [x] 运行时组合默认关闭，只从 `config.local.json` 读取私人工作区和本机 Docker 配置。
- [x] 全量自动化测试通过（183/184，1 项真实 Docker 测试默认跳过）；真实 Docker 单独验证通过（1/1）。
- [x] 完成代码简化、七类正式评审和修复后复审；所有确认的阻断项已解决。

## 阶段 2 验收结果

- 受控执行器可以在隔离副本中读取、搜索、修改和运行固定测试配置，全程不触碰源仓库。
- 工作区版本、固定测试配置、实际 Docker 镜像和审计产物相互绑定，缺失或篡改会阻止恢复与完成。
- 普通读取使用受信版本缓存；进入 Docker、显式校验和封存时仍执行完整扫描，兼顾员工浏览效率与最终完整性。
- 运行时会在向上层暴露执行器前完成恢复；配置或恢复失败直接阻止启动，不会降级为宿主命令执行。
- 第 2 阶段已分阶段本地提交完成，仍未推送 GitHub；下一阶段开始实现 GitHub 行动适配器和逐项确认队列。

## 阶段 3 验收清单

- [x] GitHub 写入默认关闭，账号、Token 环境变量名和 `gh.exe` 绝对路径只允许从本机配置注入。
- [x] 外部适配器只接受固定 Head 的 `APPROVE`、`REQUEST_CHANGES` 和 `COMMENT` Review，不提供任意 GitHub 写入入口。
- [x] 确认计划先持久化，再出现在唯一逐项弹框；浏览器只能提交修订号和绑定摘要，不能替换动作正文、账号或目标。
- [x] 执行前再次核对固定账号、PR Head 和幂等标记；正文只通过标准输入传给固定可执行文件。
- [x] 单进程队列与 Windows named pipe 单写者共同阻止并发、重复确认和双进程重复写入。
- [x] 崩溃恢复会先对账未知结果；仅已证明未发生的动作允许人工重试。
- [x] 同一 Head 下职责、下一动作或策略变化时，原岗位会先持久化失效旧确认；未知外部结果会阻止同一 PR 创建新动作。
- [x] 岗位生产端口与用户确认端口分离；HTTP 不暴露入队、失效、原始执行器或凭据。
- [x] GitHub 写入关闭时保留本地草稿接受/驳回入口；启用时只使用统一外部确认，避免双确认。
- [x] 启动、定时刷新、手动刷新和本地 PR 事实变更均先关闭确认入口；只有 GitHub PR 数据健康且岗位复核完成后才重新开放。
- [x] 服务关闭会停止定时任务、收敛在途工作并等待运行时释放，随后才能安全重启。
- [x] 全量自动化测试通过（295/296，1 项真实 Docker 测试默认跳过）；本地服务重启和真实 HTTP 接口验证通过。

## 阶段 3 验收结果

- PR 员工的三个评审结论都会转换为账号、仓库、PR 和 Head 完全绑定的确认计划；没有明确点击发布就不会调用 GitHub。
- 确认队列支持幂等批准、驳回、安全重试、生产者失效和启动恢复，旧职责或旧策略生成的 Review 不会在同一 Head 上继续发布。
- 服务只会在首次 GitHub PR 刷新和岗位复核结束后开放确认；后续每轮事实刷新期间也会临时关闭确认，数据源异常时保持关闭。
- 页面只保留一个确认弹框，依次处理外部动作、本地责任判断和关闭外部写入时的本地评审草稿；“稍后”分支不会发送写请求。
- 最新服务已在本机重新启动，PR 员工的暂停状态和 18 条历史作业均保留，外部 GitHub 写入继续保持关闭。
- 第 3 阶段只在本地提交，仍不推送 GitHub；下一阶段实现可配置工作流与路由。

## 阶段 4 验收清单

- [x] GitHub PR 和 Issue 快照转换为版本化、安全字段白名单约束的本地工作流事件。
- [x] 创建、更新、分类、状态变化、完成和离开当前范围均有确定事件；数据源异常不会覆盖最后一次健康检查点。
- [x] 路由支持组合条件、优先级、继续/停止匹配、岗位/人员/节点目标、兜底规则和环路/跳数保护。
- [x] 通配符匹配使用有界算法，并限制规则数量、模式总长度和运行工作量，恶意配置不能制造无界计算。
- [x] 每个事件、配置版本、分派结果和解释均使用内容摘要绑定并持久化；恢复时拒绝回滚、分叉和交叉引用损坏。
- [x] 持久化只允许单写者，快照游标单调推进；保留策略按完整事件包淘汰，不留下孤立审计或分派。
- [x] 状态总量限制为 32 MiB，单条审计和单个事件包也有独立上限，配置历史与检查点同样计入预算。
- [x] HTTP 只暴露只读查询和不落盘试跑，不暴露规则替换、事件生产或直接执行入口。
- [x] 页面可查看当前规则、最新分派、未命中原因和完整审计，并可用候选事件安全试跑。
- [x] 路由失败会关闭确认入口并跳过员工运行；路由本身不能调用 GitHub、确认队列或代码执行器。
- [x] 全量自动化测试通过（387/388，1 项真实 Docker 测试默认跳过），静态页面契约和真实 HTTP 接口均验证通过。

## 阶段 4 验收结果

- 当前默认规则把 PR 事件交给 `pr-reviewer`，Issue 事件交给 `requirements-analyst`，其余事件交给本人分诊；缺失岗位暂时保留为可追溯分派，不会静默丢失。
- 同一健康快照不会反复制造事件；旧快照、同时间但内容不同的快照和损坏状态都会被拒绝，异常重启不会倒退游标。
- 规则定义按整份版本替换并保留历史，试跑会返回逐条条件解释，但不会落盘或触发任何员工和外部动作。
- 路由服务已经拆分为事件契约、匹配器、状态编解码、保留策略和运行时边界，便于下一阶段接入可靠工作台账。
- 第 4 阶段只在本地提交，仍不推送 GitHub；下一阶段先把持久化分派流接入主动工作台账，再加入咨询、等待、重试和持续跟进。

## 阶段 5 验收清单

- [x] 路由分派通过单调 sequence feed 可靠进入持久化工作台账；重复 intake 不会重复创建工作项。
- [x] 工作项使用 revision、领取租约、尝试上限和指数退避；领取或落盘响应丢失时可以按持久化事实恢复。
- [x] 岗位只接收去除 owner、lease ID 和 lease deadline 的只读决策上下文；岗位解析和模型判断都有短于租约的超时。
- [x] 模型输出先经过严格结构化契约，再由可信策略绑定岗位、来源事件、仓库、PR Head 或工作区；模型不能自行扩大权限。
- [x] 咨询进入持久化内部请示箱，并与外部动作共用一个逐项弹框；用户回答绑定 request revision 与 content digest。
- [x] 回答和驳回通过 durable outbox 回写台账；回答后重新排队继续判断，驳回后明确阻塞。
- [x] 时间条件和健康、最新的工作流事实条件可以自动唤醒；不健康或过期事实保持等待。
- [x] PR 岗位继续由旧执行器独占且默认暂停；新循环不会领取、判断或重复咨询 PR 工作。
- [x] 页面新增只读工作台账和审计时间线，默认最近记录在上；HTTP 不暴露 intake、claim、stage、dispatch 或 outbox 写端口。
- [x] GitHub 写入关闭时不创建外部确认运行时；慢模型运行期间不阻塞已经安全开放的确认读取。
- [x] 全量自动化测试、语法检查、静态前端契约和真实 HTTP 接口通过。
- [x] 真实状态从 workflow schema v1 迁移到 v2，122 条分派生成 122 个唯一工作项；重复周期零新增、零重复请示。

## 阶段 5 验收结果

- 主动循环按照“事实刷新 → 路由 → intake → 回答回流 → 条件唤醒 → 岗位判断 → 意图分发”的顺序运行，定时巡检、用户回答和事实刷新使用独立串行边界。
- 真实启动后，72 条 PR 工作因 PR 岗位暂停而保持待领取；其余 Issue 分派对缺失的 `requirements-analyst` 只创建 1 个去重请示，不会用模型替用户猜接手人。
- 工作台账、内部请示和工作流分别使用独立本地单写者；组合根只向 HTTP 暴露冻结的只读台账端口和受限回答端口。
- 外部 GitHub 写入仍为关闭状态，PR 岗位 revision 66、暂停状态和 18 条历史作业全部保留。
- 第 5 阶段已分为多个本地提交，仍未推送 GitHub；第 6 阶段将把主控、需求、开发和测试岗位以及可替换的大脑接到该闭环上。

## 阶段 6 验收清单

- [x] 新增 `pr-engineer`、`orchestrator`、`requirements-analyst`、`developer` 和 `tester`，并保留旧 `pr-reviewer` 的数据与兼容接口。
- [x] 每个岗位独立持久化职责、权限、暂停状态、运行修订号、调度频率、Provider、模型和远程数据授权。
- [x] 默认使用本机 Ollama `qwen3.5:9b`；同时支持 OpenAI-compatible 第三方 Provider，密钥仅从指定环境变量读取。
- [x] 需求、代码和记忆数据默认禁止发送到远程 Provider，只有岗位按分类显式授权后才允许出机。
- [x] PR 工作流默认目标迁移到 `pr-engineer`；Issue 继续路由到 `requirements-analyst`，路由版本与历史完整保留。
- [x] 岗位能独立领取、判断、咨询、等待、移交、完成或生成受策略约束的 PR Review/代码提案；模型不能决定可信目标或执行凭据。
- [x] PR Review 提案 runner 同时约束提案类型和岗位，固定账号、仓库、PR 与 Head；只有外部动作和 GitHub 写入双开时才连接确认运行时。
- [x] 岗位页面展示每个角色的职责、权限、状态、Provider、模型和远程标记；岗位读取失败不阻塞看板与逐项确认弹框。
- [x] 事实刷新、定时巡检和手动运行使用统一串行边界；重复定时 tick 按范围合并，旧 tick 不阻塞刷新，手动运行不会被静默丢弃。
- [x] 服务先开放健康页面再完成初始刷新，但确认和岗位行动保持 fail-closed；初始失败会关闭监听并释放运行时。
- [x] 真实 Ollama 结构化判断通过；全量自动化测试最终通过（686/687，1 项真实 Docker 测试默认跳过）。
- [x] 完成独立复审并修复 Provider 参数串用、旧页面响应覆盖、新旧事实调度竞争和慢任务积压问题；复审未遗留 P0/P1/P2。

## 阶段 6 验收结果

- 本机服务当前发现 6 个岗位；5 个新岗位与旧 PR 岗位全部保持暂停，默认大脑均为本地 `ollama / qwen3.5:9b`，没有远程数据发送。
- 最新真实刷新采集 122 条 GitHub 记录和 41 条钉钉信号，源错误与大脑刷新错误均为 0；路由版本 2 已把 PR 指向 `pr-engineer`、Issue 指向 `requirements-analyst`。
- 工作台账包含 130 个唯一工作项，其中 129 个排队、1 个等待用户；统一弹框显示该本地请示，GitHub 外部动作继续关闭。
- 本地记忆索引健康并可搜索 547 条记录；岗位、工作台账和路由的最新事件已持续投影到统一记忆。
- 当时的隔离验收确认服务仅监听 `127.0.0.1:4173`；核心页面与健康、岗位、看板、确认、记忆、台账和路由接口均返回 HTTP 200，最终错误日志为空。运行特定的进程身份只保存在本地验收产物中。
- 第 6 阶段通过本地提交 `0fa997c`、`de6657a` 和 `9564ebc` 完成，仍未推送 GitHub。
- 第 7 阶段将连接代码提案、逐项授权、持久 Code Job、受控执行器工具循环和变更/测试证明回写；在此之前不能把“能提出代码提案”等同于“开发岗位已经能自动改代码”。

## 阶段 7 验收清单

- [x] 代码提案经过可信策略绑定后进入统一逐项确认队列，确认内容固定岗位、仓库、工作区、操作、可写路径、测试配置和大脑摘要。
- [x] 持久 Code Job 支持排队、运行、暂停、恢复、取消、失败、不确定和完成状态，并在每次模型调用与新动作准入前重新验证授权。
- [x] 模型只能选择读取、搜索、受控写入、固定测试配置和完成等结构化动作；不能执行任意 shell、扩大写入目录或修改源仓库。
- [x] 完成后的文件、测试和执行证据形成内容寻址的不可变 Change Package；详情页可检查清单、摘要和受校验的证据下载。
- [x] 应用 Change Package 使用独立逐项确认、Git Head/脏状态/前像冲突检查和持久回执；重复确认不会重复修改源仓库。
- [x] 取消会先持久化意图，再逐 attempt 清理隔离目录；未知写入不会重放，清理中断可在重启后继续。
- [x] Code Job 生命周期、完成/取消结果和 Change Package 应用状态投影到本地记忆与工作台账，父工作只在证据可见后进入终态。
- [x] 真实组合测试贯通路由、台账、员工提案、两次确认、代码执行、变更包、记忆、结果回收和真实 Git 应用；源仓库只在第二次确认后修改一次。
- [x] 真实 Docker 隔离测试通过；容器无网络、无敏感环境变量、源码只读且仅返回受控产物。
- [x] 全量自动化测试通过（1112/1114，2 项 Windows 符号链接测试按环境跳过），桌面与手机 UI 回归通过，独立审查无 P0/P1/P2。

## 阶段 7 验收结果

- 开发岗位已经能在用户确认的最小授权内实际读取、修改和验证代码，但执行始终发生在隔离副本中；完成不等于直接改动真实仓库。
- 用户可以先检查不可变变更包与证据，再通过第二个独立弹框决定是否应用；冲突、重复请求、响应丢失和重启均使用持久状态安全收敛。
- 真实端到端验收暴露并修复了原始员工意图 ID 与策略提案 ID 不同导致父工作无法回收的缺陷；现在以活动意图、唯一 downstreamRef 和 proposalDigest 联合绑定并 fail-closed。
- 全链路恢复验证证明取消清理中断后不会重新调用大脑、重新执行动作、生成重复变更包或污染源目录，取消记忆只写入一次。
- 阶段 7–9 的实现只保存在本地提交中，未推送 GitHub；阶段 10 的精确 PR Git 目标、跨来源 cutover、授权失效、可引用记忆权限和固定 OID 冲突准备已完成并通过独立评审，后续仍需连接 Code Job 冲突修改、验证、受控提交和强模型分流闭环。
- 精确的 owner-only PR 目标仅记录在忽略的本地验收台账中，作为系统真实能力验收任务：受控 Git 冲突/提交闭环通过镜像仓库、恢复和独立评审前不得喂给系统，也不得由开发者或助手人工查看、解决或推进；系统产生任何外部或不可逆动作前必须进入逐项弹框确认。
